<% if @hack.errors.any? %>
  <div id = "error explanation">
    <div class="alert alert-error">
      The form contains <%= pluralize(@day.errors.count, "error") %>.
    </div>
    <ul>
    <% @hack.errors.full_messages.each do |msg| %>
      <li>* <%= msg %></li>
    <% end %>
    </ul>
  </div>
<% end %>
